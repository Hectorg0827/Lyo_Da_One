/**
 * Shared Framer Motion `layoutId`s used to create shared-element transitions
 * between components that are never mounted at the same time (e.g. the nav
 * button's mascot "flying" into the chat header, and back on the way out).
 */
export const LYO_MASCOT_LAYOUT_ID = 'lyo-mascot-avatar';
